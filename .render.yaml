services:
  - type: web
    name: ippuni-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 main.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
